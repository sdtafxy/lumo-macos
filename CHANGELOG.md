# 变更记录

本文件记录所有**对外可见**的变更。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 写在这里的每一条都应该是**用户能感知**的：行为变了、观感变了、多了什么、少了什么。
> 内部重构、测试调整这类不写在这里。

## [0.0.1] — 2026-09-13

首个公开版本。

### 增强

- **五种模式**：自动增强 / 增强 / 黑白 / 对比度 / 原图。自动增强会**逐页判断**——
  文字页走黑白，含图页保色彩，所以一份「正文 + 印章 + 照片」混排的文件不必分成两次处理。
- **背景清理 0~100% 连续可调**，并按纸张实际有多脏给一个起点建议。0% 是真的关，画面一点不动。
- **纠偏**（自动检测倾斜角）、**去网纹**、**文本锐化**、**逐通道白平衡**，逐页按需启用。
- **自动裁边 + 透视校正**（默认关闭）。拍照件四周的桌面会被裁掉，歪着拍的纸会被扶正。
  默认关，因为它是整条流水线里唯一会主动丢像素的一步。
- **内容守卫**：任何滤镜把画面的直方图对比度洗掉 40% 以上就自动放弃该滤镜——
  宁可留着脏，也不能把内容洗掉。

### 识别

- **Vision OCR**，两种输出：**可搜索**（保留原版式 + 一层看不见的文字层）与
  **可编辑**（另附提取出的纯文本）。
- **已经带文字层的文件自动跳过 OCR**，不做无谓的重算。

### 压缩

- **逐页自适应路由**：单色页走 CCITT G4、灰阶与彩色页走 JPEG / JPEG2000，整份文件不必一个设置。
- **MRC 分层压缩**：背景与文字分层编码（背景低分辨率彩色 + 文字全分辨率 1 bit 蒙版 + 实测墨色），
  带**体积闸门**——只有真的更小时才采用，所以它不可能让结果变差。实测省 **97.3%**。
- **体积预估**：用真实抽样页外推出来的数字，不是公式拍出来的。

### 界面

- **单窗口承载全流程**：统一标题栏（内容延伸到窗口顶边、交通灯浮在内容之上），
  空态即全窗口拖放区，工作态左预览右参数，主按钮常驻底部状态栏。
- **处理前 / 处理后同页对照预览**，带页码输入框与从头拉到尾的滑块；
  参数一改，预览自动刷新。
- **三档玻璃质感**（不透明 / 玻璃 / 通透），在设置里一点即变。
- **深浅两套独立配色**，强调的是对比度，不是把浅色反一下。
- **内置 PDF 阅读器**：设计文档与示例扫描件直接在 App 里打开，带页码输入、翻页滑块与缩放。
- **输入入口宽容**：拖到窗口任意处、拖到 Dock 图标、`⌘O`、`⇧⌘V` 粘贴文件、底栏按钮、菜单。
- 窗口有最小尺寸限制，缩到底也不会把预览压变形。

### 自动更新

- 从 GitHub Releases 检查新版本，**点一下就原地换掉并自动重启**；换包失败自动回滚。
- 用户数据全在 App 包之外，所以「无损更新」是结构上的保证，不是小心翼翼的结果。
- 校验分三级并**如实标注**：HTTPS → SHA-256 完整性 → Ed25519 签名（当前未启用，见 README 的已知限制）。
- 「自动检查」默认开、「自动下载并安装」默认关（它会重启 App，那是要用户点头的事）。

### 命令行

- `lumo-cli` 与 App 共用同一份引擎：`report`（体检）、`process`（处理）、`demo`（生成对照图）、
  `selftest`（在真机上跑完整流水线并断言结果）。

### 隐私

- 处理全部在本机完成，不上传文件、文件名或任何统计。
- 唯一的网络行为是自动更新检查；关掉之后一行请求都不发。

[0.0.1]: https://github.com/sdtafxy/lumo-macos/releases/tag/v0.0.1
