# 安全策略

## 支持范围

只有**最新发布版本**会收到安全修复。项目处于 0.x 阶段，不维护旧版本分支。

| 版本 | 支持 |
| --- | --- |
| 最新 release | ✅ |
| 更早 | ❌ |

## 报告漏洞

请**不要**开公开 issue。用 GitHub 的私密报告通道：

**Security → Report a vulnerability**（<https://github.com/sdtafxy/lumo-macos/security/advisories/new>）

请在报告里说明：影响版本、复现步骤、以及你认为的影响面。
我会尽快回复；修复发布后会在 advisory 里署名（除非你希望匿名）。

## 这个项目的攻击面（帮你判断什么算漏洞）

Lumo 是一个**本机处理 PDF 的工具**，它不做上传、不做账号、不做同步。
所以它的攻击面比一般联网应用窄很多，主要是这几处：

| 面 | 说明 |
| --- | --- |
| **PDF 解析** | 输入文件是不可信数据。解析器（ImageIO / CoreGraphics / Vision / PDFKit）里的崩溃或越界属于系统框架层；但如果**我们自己的代码**在解析畸形 PDF 时写出了越界、无限循环或路径穿越，那是本项目的问题 |
| **自动更新** | 唯一的联网功能。见下 |
| **网页内容** | 无。App 里没有内嵌浏览器、没有 WebView、不加载远程内容 |

### 自动更新的强度（如实说明）

| 手段 | 能防什么 | **防不住什么** |
| --- | --- | --- |
| HTTPS | 链路上被改包 | 更新源被攻破后替换 Release |
| SHA-256 | 下载截断、传输损坏 | **能改包的人也能改那个 `.sha256`** |
| Ed25519（App 内置公钥验签） | 上述全部 | 私钥本身泄露 |

**关键认知：SHA-256 是「完整性」检查，不是「来源」检查。**
校验和与安装包放在同一个 Release 里，能替换安装包的人就能替换校验和。

当前 **Ed25519 验签尚未启用**（`Resources/Info.plist` 的 `LumoUpdatePublicKey` 为空），
所以现在只做到 SHA-256 那一级。App 会在设置页把校验级别**如实标出来**，
不会含糊地说一句「已校验」。开启方式见 README。

因此：**「能在没配公钥的情况下让 App 装上被替换的包」是当前已知的、被记录的强度选择，
不是漏洞报告的目标。** 但如果你发现的是下面这些，请一定报告：

- 校验在失败时**静默降级**了（例如哈希不符却仍然安装）；
- 更新过程中的 **TOCTOU**（校验的和安装的不是同一份数据）；
- 换包过程中的路径穿越、符号链接攻击，或能让 App 的**可写性预检**被绕过；
- 失败回滚本身会破坏用户数据；
- 更新助手（helper）能被以超出预期的权限或时机调用。

## 我不会做的事

- 不会为了"看起来更安全"而在 UI 上说假话（比如把「只校验了哈希」写成「已验证签名」）。
- 不会把用户文件上传到任何地方。
